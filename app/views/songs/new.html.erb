<%= form_for(@song) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.@artist.id.label :@artist.id %>
  <%= f.@artist.id.text_field :@artist.id %>
  <%= f.genre.id.label :genre.id %>
  <%= f.genre.id.text_field :genre.id %>

  <%= f.submit "Update Song"%>
<% end %>
