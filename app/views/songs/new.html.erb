<%=form_tag("/songs") do%>

<%=label_tag('song[name]', "My song name")%>
<%=text_field_tag('song[name]') %>

<%=label_tag('song_params[artist_id]', @artist.id)%>
<%=text_field_tag('song_params[artist_id]')%>

<%=label_tag('song_params[genre_id]', @genre.id)%>
<%=text_field_tag('song_params[genre_id]')%>

  <%= submit_tag "Create Song" %>
  <% end %>
