<%= form_for(@song) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.artist_id.label :artist.id %>
  <%= f.artist_id.text_field :artist.id %>
  <%= f.genre_id.label :genre.id %>
  <%= f.genre_id.text_field :genre.id %>

  <%= f.submit %>
<% end %>
